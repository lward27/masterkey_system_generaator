#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='masterkey_system_generator',
      version='1.0',
      description='An API used to generate master key systems',
      author='Lucas Ward',
      author_email='lucas@lucas.engineering',
      url='https://us.ippon.tech/',
      packages=find_packages(),
     )
